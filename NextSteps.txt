Must:
1. is_open_event_matches_rule
2. Test 1. 4 rules of execve and 4 rules of open. 2 open 2 openat.
3. test Whitelist process
4. delete rules ioctl
5. Delete exclusions ioctl
6. UserSpace
7. Python script that tests the shit out of UserSpace and Kmod.


Possible
1. slab 
2. Different lists for open and excve rules
3. Cache
4. cd /tmp; nano /tmp/newFile.txt; will cause target_path to be 'newFile.txt'. Only scenario that it happend (tried many different).
   - if target_path doesn't start with '/' , translate AT_FDCWD and insert it in the begining of the path. 